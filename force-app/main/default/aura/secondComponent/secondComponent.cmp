<aura:component >
	<!--Receiver -->
    <aura:attribute name="finalMessage" type="String" />
    
    {! v.finalMessage }
    
    <aura:handler event="c:understandAppEvent" action="{! c.handleApplicationEvent }" />
</aura:component>